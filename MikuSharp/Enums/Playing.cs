namespace MikuSharp.Enums
{
    public enum Playstate
    {
        NotPlaying = 0,
        Playing = 1,
        Paused = 2,
        Stopped = 3
    }
    public enum RepeatMode
    {
        Off = 0,
        On = 1,
        All = 2
    }
    public enum ShuffleMode
    {
        Off = 0,
        On = 1
    }
}
